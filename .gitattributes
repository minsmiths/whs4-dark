# 셸 스크립트는 항상 LF로 저장 — Windows core.autocrlf=true가 CRLF로 바꾸면
# 컨테이너 안에서 "#!/bin/sh\r" 셔뱅을 인터프리터 경로로 오인해 실행이 깨진다
# (exec ./docker/login.sh: no such file or directory). 관련: progress-log 참고.
*.sh text eol=lf
