# CLAUDE.md

이 파일은 Claude Code(또는 다른 AI 보조 도구)가 이 프로젝트에서 작업할 때 항상 참조하는
프로젝트 계약서입니다. 요구사항·준수사항·시스템 구조·Collector 규격·MD 리포트 포맷의 **원본**이며,
`docs/` 아래 문서들은 이 파일을 참조만 하고 값을 복사하지 않습니다.

원본 기획서: `다크웹 크롤링 계획서 및 지시 (v0.6)`. 로드맵(M0~M6)은 [docs/development-plan.md](docs/development-plan.md),
PR/배포 전 체크리스트는 [docs/security-checklist.md](docs/security-checklist.md), 보안 위협 대응표는
[docs/threat-model.md](docs/threat-model.md)로 분리되어 있습니다.

---

## 1. 배경 및 목적

다크웹(Tor 히든서비스) 포럼·사이트를 사람이 직접 방문해 통계·현황을 파악하는 작업은 `.onion` 라우팅
특성상 페이지 응답이 느려 시간이 오래 걸립니다. 이 프로젝트는 조사 과정을 크롤러로 자동화하여,
대상 사이트 1곳의 URL을 입력하면 7개 항목(가용성/구조/통계/콘텐츠 표본/교차참조/접근성/유저 활동)을
자동으로 수집·정리한 결과를 마크다운(MD) 파일로 산출합니다.

**한 줄 요약**: 승인된 다크웹 포럼 URL 하나를 넣으면, 격리된 Docker 컨테이너 안에서
로그인·크롤링·분석을 전부 자동으로 수행하고, 노션에 옮겨 적을 수 있는 조사 초안 MD 파일 하나를
산출하는 CLI 도구. 판단이 필요한 부분(OFFLINE 확정, 동일인 판정, 한국 관련 유출 확정 등)은 전부
"후보"까지만 채우고 최종 확정은 사람이 합니다 — **"스카우트 로봇"**.

| 입력 | URL + 사이트 유형(`forum` 등, 사람이 지정) |
| --- | --- |
| 출력 | `output/<source_id>_<timestamp>.md` (23개 칸 구조를 따르는 조사 초안 1개) |

### 동작 흐름

1. 화이트리스트 확인 (등록 안 된 URL이면 거부)
2. (최초 1회) 로그인 필요하면 사람이 VNC로 컨테이너 화면 보며 직접 로그인 → 세션 저장
3. Tor 프록시 + Playwright(샌드박스 유지)로 대상 접속, Cloudflare 챌린지는 사람이 VNC로 1회 통과
4. Collector 7단계 순서대로 수집: 가용성 → 구조 → 통계 → 콘텐츠 표본 → 교차참조 → 접근성 프로빙 → 유저 활동
5. 각 값은 확인함 / 없음 / 못 봄(이유) / 안 봄 중 하나로 명시적으로 기록 (빈칸으로 안 남김)
6. 노션 칸 구조 그대로 MD 파일 생성
7. 컨테이너 밖으로는 그 MD 파일 하나만 반출 (원문 스냅샷·쿠키·로그는 컨테이너 안에만)
8. 사람이 MD를 보고 노션 "다크웹 조사 가이드"에 옮겨 적으며 검토

**MVP에서 뺀 것**: Notion 실시간 API 연동, 사이트 간 자동 병합, 다중 사이트 스케줄러, 대시보드 GUI,
위협 인텔 알림(Phase 2). → [docs/development-plan.md](docs/development-plan.md) §확장 단계 참고.

---

## 2. 요구사항

1. 로그인 상태를 유지한 채 크롤링
2. Cloudflare 등 안티봇 인증 통과
3. 다크웹 사이트의 구조 파악
4. 사이트 활성화 정도 파악
5. 필터 조건에 따른 게시글 통계
6. 사이트 운영 방식 파악
7. 특정 유저의 활동 분석

기능별 구현 가이드(담당 모듈, 코드 스니펫)는 §6을 참고하세요.

---

## 3. 준수사항 (위반 시 프로젝트 원칙 위반)

1. 승인된 대상 URL 1곳에 한해서만 동작한다 (`whitelist.yaml`).
2. 수동적 관찰만 수행한다 — 게시글 작성, 댓글, 신고, 거래, 회원가입 등 능동적 행위는 코드에 구현하지 않는다.
3. 로그인이 필요한 대상은 사람이 리서치 계정으로 직접 로그인하고, 크롤러는 그 세션만 재사용한다.
   자동 재로그인·자동 가입 기능은 만들지 않는다.
4. 개인정보(PII) 원문은 저장하지 않는다 — 존재 여부와 유형만 기록한다 (예: `contains_email: true`).
5. 유출 파일·첨부파일 등 다운로드 링크가 발견되면 존재 여부만 기록하고 파일 자체는 절대 다운로드하지 않는다.
6. 아동 성착취물 등 명백한 불법 콘텐츠가 발견되면 크롤링을 즉시 중단하고, 원문은 저장하지 않고
   URL과 발견 시각만 기록한 뒤 담당자에게 즉시 보고한다.
7. 모든 요청(URL, 시각, 응답 코드)을 로그 파일에 남긴다.
8. 개발 과정에서 AI(Claude 등)는 실제 대상 사이트에 대한 로그인 시도, CAPTCHA 우회, 회원가입,
   실 계정 사용을 직접 수행하지 않는다. 개발/테스트는 공개 페이지 또는 로컬 fixture로 진행하고,
   실제 대상 접속은 담당자가 지켜보는 자리에서 1회 검증한다.

---

## 4. 시스템 구조

### 4.1 격리된 실행 환경

크롤러는 Docker 컨테이너 1개 안에서 실행한다(Tor·Python·Playwright를 모두 포함).

- 아웃바운드 네트워크는 Tor SOCKS5 프록시(기본 `127.0.0.1:9050`)를 통해서만 나간다.
- 이 컨테이너에는 Notion API 토큰, GitHub 개인 액세스 토큰 등 외부 서비스 자격증명을 설치하지 않는다.
- 컨테이너 밖으로 나가는 파일은 **결과 MD 파일 1개**뿐이다. 원문 스냅샷(raw HTML), 쿠키, 로그는
  컨테이너 내부(named volume)에만 남고 자동으로 반출되지 않는다.
- 컨테이너는 언제든 폐기·재생성 가능한 상태로 유지한다(`docker run --rm`). 의심스러운 동작이 있으면
  컨테이너를 삭제하고 이미지를 다시 빌드해 새로 시작한다.

### 4.2 브라우저·네트워크 보안 규칙

1. Chromium 실행 시 `--no-sandbox`, `--disable-setuid-sandbox` 플래그를 사용하지 않는다.
2. 로그인처럼 사람이 화면을 직접 보고 조작해야 하는 작업도 **컨테이너 안에서 VNC로 화면에 접속해** 수행한다.
   호스트가 다크웹 페이지를 직접 렌더링하는 경로를 만들지 않는다.
3. Tor SOCKS5 포트(9050)를 호스트에 노출해야 하는 경우 `127.0.0.1:9050:9050`처럼 로컬호스트에만
   바인딩한다. `0.0.0.0`으로 열지 않는다.
4. 유출 파일·샘플 다운로드 링크는 존재 여부만 기록하고 다운로드하지 않는다(§3-5).
5. `cf_clearance` 쿠키는 발급받은 IP에 묶여 있고 유효시간이 짧다(대개 30분 내외). 크롤 세션 동안
   Tor 회선(exit IP)을 고정한다(`torrc`의 `MaxCircuitDirtiness`를 예: 1800초로 상향).
6. 챌린지 감지는 최초 로그인 시점뿐만 아니라 **크롤링 도중 모든 요청에서 상시로 수행**한다.
   재발해도 자동 우회 코드는 만들지 않고 그때마다 VNC로 사람이 다시 푼다.

**방어 범위 밖**: 국가 수준 공격자의 0-day를 이용한 컨테이너·호스트 탈출, Docker·Chromium 자체의
알려지지 않은 취약점, 물리적 접근을 통한 공격. 대신 "뚫려도 피해가 크지 않게"(자격증명 없음,
일회성 컨테이너, 결과물은 MD 파일 하나만 반출) 원칙으로 피해를 한정한다.
보안 위협 시나리오별 대응표는 [docs/threat-model.md](docs/threat-model.md) 참고.

---

## 5. Collector 공통 규격

Collector는 대상 사이트에 접속해서 원문(HTML/텍스트)을 가져오고 그 자리에서 필요한 값을 뽑아내는
모듈입니다 (MVP는 별도 Parser 레이어로 분리하지 않고 각 Collector 파일 안에서 파싱까지 처리).

```python
def run(page: playwright.sync_api.Page, source: dict) -> dict:
    """
    반환값 예시:
    {
        "상태": {"value": "online", "observed_at": "2026-08-09", "source": "직접 접속"},
        "규모": {"state": "BLOCKED", "reason": "로그인 필요"},
    }
    """
```

값은 항상 아래 형태 중 하나로 반환한다:

- `{"value": v, "observed_at": d, "source": s}` — 값을 확인함
- `{"state": "CONFIRMED_ABSENT"}` — 확인했는데 없음
- `{"state": "BLOCKED", "reason": r}` — 막혀서 못 봄
- 키 자체를 반환 dict에 넣지 않음 → Report Generator가 "안 봄"으로 처리

---

## 6. 요구사항별 구현 가이드

| 요구사항 | 담당 모듈 | 핵심 규칙 |
| --- | --- | --- |
| 1. 로그인 상태 유지 | `session_manager.py` | VNC로 사람이 최초 로그인 → `storage_state` 저장/재사용. 세션 만료 시 자동 재로그인 금지, `BLOCKED("세션 만료")`로 기록 |
| 2. 안티봇 통과 | `config.py` + 공통 | Tor SOCKS5 프록시, 샌드박스 유지. "Just a moment" 등 챌린지 감지 시 VNC로 사람이 풂. 요청 간 3~5초 랜덤 지연 |
| 3. 사이트 구조 | `collectors/structure.py`, `collectors/cross_reference.py` | nav/메뉴 1단계 depth만 수집. 외부 URL·`.onion`·`t.me/` 언급은 "발견"으로만 기록, 자동 확정 안 함 |
| 4. 활성화 정도 | `collectors/stats.py` | 통계 영역 파싱 실패 시 페이지네이션으로 추정. **최근 게시일**이 활성도의 실질 지표 |
| 5. 필터 기반 통계 | `collectors/content_sample.py` | 표본 N건(기본 50) 기준. "샘플 N건 기준, 전체 아님" 명시 필수 |
| 6. 운영 방식 | `structure.py` 결과 재사용 | AI 요약 쓰지 않음. 원문 그대로 인용, 요약은 사람이 작성 |
| 7. 유저 활동 | `collectors/user_activity.py` | 핸들별 건수만 집계. 동일인 판단 안 함 — "판단 근거/반대 근거" 빈칸을 사람이 채움 |

세부 코드 스니펫과 selector 조사 방법은 각 `collectors/*.py` 파일의 docstring에 있습니다.

---

## 7. MD 리포트 생성 규칙 (`report_generator.py`)

Collector들이 반환한 dict를 모아 노션 "다크웹 조사 가이드"의 23개 칸 구조 그대로 MD 파일을 만듭니다.

### 7.1 값 포맷

| Collector 반환 상태 | MD 출력 |
| --- | --- |
| `value / observed_at / source` 있음 | 값 (날짜, 출처) |
| `state: CONFIRMED_ABSENT` | 없음 |
| `state: BLOCKED, reason: r` | 못 봄. r (오늘 날짜) |
| 키 자체가 없음 | 안 봄 |

상대 시간 표현("최근", "현재")은 쓰지 않는다 — 항상 절대 날짜.

### 7.2 절대 자동 채움 금지 필드

`웹에 올림`, `조사 단계`, `담당자`, `국가`, `유통 자리`, `이전 이름·별칭`(확정 부분), `연결된 곳`(확정 부분)
— 이 칸들은 MD에서 항상 빈 칸 또는 "안 봄"으로 둔다. `report_generator.py`의 `FORBIDDEN_AUTO_FIELDS`
상수로 하드코딩되어 있으며, `tests/test_report_generator.py`가 이 규칙을 전용으로 검증한다.

---

## 8. 개발/리뷰 시 주의사항 (AI 보조 도구 대상)

- 요청받지 않은 기능(§1 MVP에서 뺀 것 포함)을 임의로 추가하지 마세요.
- §3 준수사항에 위배되는 코드를 요청받으면, 작성하기 전에 위반 사항을 알려주세요.
- `report_generator.py`의 `FORBIDDEN_AUTO_FIELDS`, `collectors/`의 능동적 행위 금지(§3-2),
  `Dockerfile`/브라우저 launch 옵션의 샌드박스 규칙(§4.2-1)은 **코드 리뷰로만 막을 수 있는 안전장치**입니다.
  이 경로를 건드리는 변경은 각별히 주의하고 CODEOWNERS 리뷰를 거치세요.
- 실제 대상 사이트 접속·로그인·CAPTCHA 우회를 AI가 직접 수행하지 않습니다(§3-8).
